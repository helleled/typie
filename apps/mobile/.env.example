# Typie Mobile 환경 변수 설정
# 
# 이 파일을 .env로 복사하여 사용하세요:
# cp .env.example .env
#
# 🚀 오프라인 우선 개발 환경
# 참고: Flutter에서 환경 변수를 사용하려면 빌드 시 설정이 필요합니다.
#
# 📱 에뮬레이터/시뮬레이터 접속 설정:
# - Android 에뮬레이터: 10.0.2.2 사용
# - iOS 시뮬레이터: localhost 사용

# =============================================================================
# API 서비스 URL
# =============================================================================

# 인증 서비스 URL (오프라인 모드 기본값)
AUTH_URL=http://localhost:5173

# API 서버 URL (오프라인 모드 기본값)
API_URL=http://localhost:8080

# WebSocket URL (오프라인 모드 기본값)
WS_URL=ws://localhost:8080

# 사용자 사이트 URL (오프라인 모드 기본값)
USERSITE_URL=http://localhost:5173

# 웹사이트 URL (오프라인 모드 기본값)
WEBSITE_URL=http://localhost:5173

# =============================================================================
# OAuth 클라이언트 설정 (오프라인 모드 기본값)
# =============================================================================

# OIDC 클라이언트 (오프라인 모드 기본값)
OIDC_CLIENT_ID=dev-client-id
OIDC_CLIENT_SECRET=dev-client-secret

# 외부 OAuth (오프라인 모드에서는 비활성화됨)
# GOOGLE_CLIENT_ID=
# GOOGLE_SERVER_CLIENT_ID=
# KAKAO_NATIVE_APP_KEY=
# NAVER_CLIENT_ID=
# NAVER_CLIENT_SECRET=

# =============================================================================
# 분석 및 모니터링 (선택 사항 - 오프라인 모드에서는 비활성화)
# =============================================================================

# 분석 및 모니터링 (프로덕션 환경에서만 설정)
# APPSFLYER_KEY=
# MIXPANEL_TOKEN=
# SENTRY_DSN=
