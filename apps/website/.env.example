# Typie Website 환경 변수 설정
# 
# 이 파일을 .env로 복사하여 사용하세요:
# cp .env.example .env
#
# 🚀 오프라인 우선 개발 환경
# 로컬 API 서버에 자동 연결됩니다.

# =============================================================================
# Public 환경 변수 (브라우저에서 접근 가능)
# =============================================================================

# API 서버 URL (오프라인 모드 기본값)
PUBLIC_API_URL=http://localhost:8080

# 인증 서비스 URL
PUBLIC_AUTH_URL=http://localhost:5173

# CMS 설정 (선택 사항)
PUBLIC_CMS_URL=
PUBLIC_CMS_STAGE=

# 환경 설정
PUBLIC_ENVIRONMENT=local

# 오프라인 모드 활성화 (개발 환경 기본값)
PUBLIC_OFFLINE_MODE=true

# 사용자 사이트 호스트
PUBLIC_USERSITE_HOST=localhost:5173

# 웹사이트 URL
PUBLIC_WEBSITE_URL=http://localhost:5173

# WebSocket URL
PUBLIC_WS_URL=ws://localhost:8080

# OIDC 클라이언트 설정
PUBLIC_OIDC_CLIENT_ID=dev-client-id

# =============================================================================
# 분석 및 모니터링 (선택 사항 - 오프라인 모드에서는 비활성화)
# =============================================================================

# 분석 및 모니터링 (프로덕션 환경에서만 설정)
# PUBLIC_MIXPANEL_TOKEN=
# PUBLIC_SENTRY_DSN=

# =============================================================================
# Private 환경 변수 (서버 사이드만 접근 가능)
# =============================================================================

# 내부 API URL (서버 사이드에서 사용)
PRIVATE_API_URL=http://localhost:8080

# OIDC 클라이언트 시크릿
PRIVATE_OIDC_CLIENT_SECRET=dev-client-secret
