import type { DashboardLayout_Query_AfterLoad } from './$graphql';

export const _DashboardLayout_Query_AfterLoad: DashboardLayout_Query_AfterLoad = () => {
  // No authentication check needed for local-only mode
};

export const ssr = false;
