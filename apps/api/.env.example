# Typie API 환경 변수 설정
# 
# 이 파일을 .env로 복사하여 사용하세요:
# cp .env.example .env
#
# 또는 루트의 .env.local을 사용하세요. (권장)
#
# 🚀 오프라인 우선 개발 환경
# 별도의 외부 서비스 없이 바로 개발을 시작할 수 있습니다.

# =============================================================================
# 기본 환경 변수 (오프라인 모드 기본값)
# =============================================================================

# 데이터베이스 경로 (SQLite - 오프라인 모드 기본값)
DATABASE_URL=./data/typie.db

# 오프라인 모드 활성화 (개발 환경 기본값)
OFFLINE_MODE=true

# Redis 연결 (오프라인 모드에서는 인메모리 캐시 사용)
# REDIS_URL=redis://localhost:6379

# =============================================================================
# 서버 설정
# =============================================================================

# API 서버 포트
LISTEN_PORT=8080

# 서비스 URL
AUTH_URL=http://localhost:5173
WEBSITE_URL=http://localhost:5173
USERSITE_URL=http://localhost:5173

# =============================================================================
# 검색 엔진 (선택 사항 - 오프라인 모드에서는 비활성화)
# =============================================================================

# Meilisearch (프로덕션 환경에서만 사용)
# MEILISEARCH_URL=http://localhost:7700
# MEILISEARCH_API_KEY=masterKey

# =============================================================================
# OAuth 인증 (오프라인 모드에서는 더미 값만 필요)
# =============================================================================

# OIDC 기본 설정 (오프라인 모드 기본값)
OIDC_CLIENT_ID=dev-client-id
OIDC_CLIENT_SECRET=dev-client-secret
OIDC_JWK={"kty":"RSA","n":"dummy","e":"AQAB"}

# 외부 OAuth (오프라인 모드에서는 비활성화됨)
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# KAKAO_CLIENT_ID=
# KAKAO_CLIENT_SECRET=
# NAVER_CLIENT_ID=
# NAVER_CLIENT_SECRET=

# =============================================================================
# Apple 서비스 (오프라인 모드에서는 비활성화됨)
# =============================================================================

# Apple 서비스 (프로덕션 환경에서만 설정)
# APPLE_TEAM_ID=
# APPLE_SIGN_IN_KEY_ID=
# APPLE_SIGN_IN_PRIVATE_KEY=
# APPLE_APP_APPLE_ID=0
# APPLE_APP_BUNDLE_ID=
# APPLE_IAP_ISSUER_ID=
# APPLE_IAP_KEY_ID=
# APPLE_IAP_PRIVATE_KEY=

# =============================================================================
# 결제 서비스 (오프라인 모드에서는 비활성화됨)
# =============================================================================

# 결제 서비스 (프로덕션 환경에서만 설정)
# PORTONE_API_SECRET=
# PORTONE_CHANNEL_KEY=
# GOOGLE_PLAY_PACKAGE_NAME=
# GOOGLE_SERVICE_ACCOUNT={}

# =============================================================================
# 외부 API 서비스 (오프라인 모드에서는 비활성화됨)
# =============================================================================

# 외부 API (프로덕션 환경에서만 설정)
# ANTHROPIC_API_KEY=
# GITHUB_TOKEN=
# IFRAMELY_API_KEY=
# SLACK_BOT_TOKEN=
# SLACK_SIGNING_SECRET=
# SLACK_WEBHOOK_URL=
# SPELLCHECK_API_KEY=
# SPELLCHECK_URL=http://localhost:8081

# =============================================================================
# 모니터링 (선택 사항)
# =============================================================================

# 모니터링 서비스 (프로덕션 환경에서만 설정)
# SENTRY_DSN=
# OTEL_EXPORTER_OTLP_ENDPOINT=
